<ResourceDictionary mc:Ignorable="cs"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:charts="http://schemas.devexpress.com/winfx/2008/xaml/charts"
                    xmlns:core="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    xmlns:cs="colorscheme_ignorable_uri"
                    xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/sankey/themekeys"
                    xmlns:local="http://schemas.devexpress.com/winfx/2008/xaml/sankey"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <charts:BoolToVisibilityConverter x:Key="boolToVisibilityConverter"/>

    <DataTemplate x:Key="defaultTooltipContentTemplate">
        <TextBlock Foreground="{DynamicResource {dxt:SankeyBrushesThemeKey ResourceKey=ToolTipForeground}}"
                   HorizontalAlignment="Center"
                   Text="{Binding ToolTipText}"/>
    </DataTemplate>

    <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=DevExpress.Xpf.Charts.Sankey.SankeyDiagramControl}" TargetType="{x:Type local:SankeyDiagramControl}">
        <Setter Property="ClipToBounds" Value="True"/>
        <Setter Property="Background" Value="{DynamicResource {dxt:SankeyBrushesThemeKey ResourceKey=BackgroundBrush}}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource {dxt:SankeyBrushesThemeKey ResourceKey=BorderBrush}}"/>
        <Setter Property="BorderThickness" Value="{DynamicResource {dxt:SankeyDiagramThemeKey ResourceKey=BorderThickness}}"/>
        <Setter Property="Padding" Value="{DynamicResource {dxt:SankeyDiagramThemeKey ResourceKey=PaddingValue}}"/>
        <Setter Property="FocusManager.IsFocusScope" Value="True"/>
        <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Cycle"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Once"/>
        <Setter Property="AutomationProperties.Name" Value="SankeyDiagramControl"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SankeyDiagramControl}">
                    <Grid>
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <local:SankeyTitleItemsControl x:Name="PART_TitleItemsContainer" Titles="{Binding Path=Titles, RelativeSource={RelativeSource TemplatedParent}}">
                                    <local:SankeyTitleItemsControl.LastChild>
                                        <local:SankeyElementsPanel x:Name="PART_ElementsContainer">
                                            <local:SankeyLinkItemsControl x:Name="PART_LinkItemsContainer"/>
                                            <local:SankeyNodeItemsControl x:Name="PART_NodeItemsContainer"/>
                                            <local:SankeyNodeLabelItemsControl x:Name="PART_NodeLabelItemsContainer"/>
                                        </local:SankeyElementsPanel>
                                    </local:SankeyTitleItemsControl.LastChild>
                                </local:SankeyTitleItemsControl>
                                <ContentPresenter x:Name="PART_TopContent"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Visibility="Hidden"/>
                            </Grid>
                        </Border>
                        <local:SankeyToolTipPanel x:Name="PART_ToolTipPanel"
                                                  Focusable="False"
                                                  IsHitTestVisible="False"
                                                  Visibility="{Binding Path=ActualToolTipEnabled, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource boolToVisibilityConverter}}">
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   HorizontalOffset="{Binding Path=ToolTipItem.Position.X, RelativeSource={RelativeSource TemplatedParent}}"
                                   IsOpen="{Binding Path=ToolTipItem.Visible, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                   Placement="Absolute"
                                   VerticalOffset="{Binding Path=ToolTipItem.Position.Y, RelativeSource={RelativeSource TemplatedParent}}">
                                <ContentPresenter x:Name="PART_ToolTip"
                                                  Content="{Binding Path=ToolTipItem, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ContentTemplate="{Binding Path=ToolTipTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"/>
                            </Popup>
                        </local:SankeyToolTipPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="NodeTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Fill="{Binding Brush}" OpacityMask="{Binding OpacityMask}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ToolTipTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Background="Transparent"
                          UseLayoutRounding="True"
                          cs:id="Grid_0000">
                        <Border Background="{DynamicResource {dxt:SankeyBrushesThemeKey ResourceKey=ToolTipBorder}}" CornerRadius="4"/>
                        <Border Background="{DynamicResource {dxt:SankeyBrushesThemeKey ResourceKey=ToolTipBackground}}"
                                core:BorderExtensions.ClipChild="True"
                                CornerRadius="3"
                                Margin="1">
                            <ContentPresenter x:Name="PART_ToolTipContent"
                                              Content="{Binding Info}"
                                              ContentTemplate="{Binding Path=DataContext.ContentTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                              Margin="{DynamicResource {dxt:SankeyDiagramThemeKey ResourceKey=ToolTipContentMargin}}"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="NodeToolTipContentTemplate" Value="{StaticResource defaultTooltipContentTemplate}"/>
        <Setter Property="LinkToolTipContentTemplate" Value="{StaticResource defaultTooltipContentTemplate}"/>
    </Style>

</ResourceDictionary>